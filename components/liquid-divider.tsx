/*
  DEPRECATED — no longer used.
  The dark→white transition is now handled by the scroll-driven reveal in
  components/scroll-reveal.tsx (StickyBackdrop + WhiteSheet). This file is
  kept only because the sandbox can't delete files; feel free to remove it.
*/
export function LiquidDivider() {
  return null
}
